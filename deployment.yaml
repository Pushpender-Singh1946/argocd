apiVersion: apps/v1
kind: Deployment
metadata:
  name: myappnginx

spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      app: myapp
    spec:
      containers:
      - name: myapp
        image: nginx
        ports:
        - containerPort: 80
